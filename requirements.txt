# Coding Assistant Agent — Requirements
# Install with: pip install -r requirements.txt

# LangGraph — the graph execution engine for our agent
langgraph==1.2.4

# LangChain core — message types, tool decorator, base abstractions
# langgraph depends on this, pinning so nothing drifts
langchain-core==1.4.0

# Groq integration for LangChain — gives us ChatGroq
langchain-groq==1.1.2

# Loads GROQ_API_KEY from .env file
python-dotenv==1.2.2

# FastAPI — serves the agent as a REST API
fastapi==0.136.3

# Uvicorn — ASGI server that runs FastAPI
uvicorn==0.48.0

# Streamlit — Python-native UI frontend
streamlit==1.58.0